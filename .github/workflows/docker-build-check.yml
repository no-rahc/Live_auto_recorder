name: Docker multi-platform check

on:
  pull_request:
    paths:
      - 'Dockerfile'
      - '.dockerignore'
      - 'compose.yaml'
      - '.env.example'
      - 'requirements.txt'
      - 'app_entry.py'
      - 'lar_app/**'
      - 'module/**'
      - '.github/workflows/docker-build-check.yml'
  push:
    branches: [main]
    paths:
      - 'Dockerfile'
      - '.dockerignore'
      - 'compose.yaml'
      - '.env.example'
      - 'requirements.txt'
      - 'app_entry.py'
      - 'lar_app/**'
      - 'module/**'
      - '.github/workflows/docker-build-check.yml'

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v7
      - uses: docker/setup-qemu-action@v4
      - uses: docker/setup-buildx-action@v4

      - name: Build amd64 smoke image
        uses: docker/build-push-action@v7
        with:
          context: .
          file: ./Dockerfile
          platforms: linux/amd64
          load: true
          tags: live-auto-recorder:smoke
          cache-from: type=gha

      - name: Smoke test health and dashboard
        shell: bash
        run: |
          set -euo pipefail
          container="live-auto-recorder-smoke"
          port=15000

          cleanup() {
            docker logs "$container" 2>&1 || true
            docker rm -f "$container" >/dev/null 2>&1 || true
          }
          trap cleanup EXIT

          docker run -d --name "$container" \
            -p "127.0.0.1:${port}:5000" \
            -e ALLOW_ANONYMOUS=true \
            live-auto-recorder:smoke >/dev/null

          ready=false
          for attempt in $(seq 1 60); do
            if curl -fsS "http://127.0.0.1:${port}/healthz" >/dev/null; then
              ready=true
              break
            fi
            sleep 2
          done
          if [[ "$ready" != true ]]; then
            echo "Container did not become healthy"
            exit 1
          fi

          status="$(curl -sS -o /tmp/dashboard.html -w '%{http_code}' "http://127.0.0.1:${port}/")"
          if [[ "$status" != "200" ]]; then
            echo "Dashboard returned HTTP ${status}"
            cat /tmp/dashboard.html || true
            exit 1
          fi
          grep -qiE '<!doctype html|<html' /tmp/dashboard.html

      - name: Build amd64 and arm64 images
        uses: docker/build-push-action@v7
        with:
          context: .
          file: ./Dockerfile
          platforms: linux/amd64,linux/arm64
          push: false
          cache-from: type=gha
          cache-to: type=gha,mode=max
