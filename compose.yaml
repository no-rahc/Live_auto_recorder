services:
  recorder:
    image: ${LIVE_AUTO_RECORDER_IMAGE:-yeowoonlee/live-auto-recorder:latest}
    build:
      context: .
      dockerfile: Dockerfile
    container_name: live-auto-recorder
    init: true
    restart: unless-stopped
    ports:
      # Local-only console: do not publish the web port on external interfaces.
      - "127.0.0.1:${APP_PORT:-5000}:5000"
    environment:
      TZ: ${TZ:-Asia/Seoul}
      # The process listens on the container interface; the host publication above remains loopback-only.
      HOST: 0.0.0.0
      PORT: 5000
      LOG_LEVEL: ${LOG_LEVEL:-info}
      ALLOW_SECRET_BACKUPS: ${ALLOW_SECRET_BACKUPS:-false}
    volumes:
      - ${CONFIG_PATH:-./data}:/app/json
      - ${RECORDINGS_PATH:-./recordings}:/app/chzzk
      - ${LOG_PATH:-./logs}:/app/logs
      - ${TMP_PATH:-./tmp}:/app/tmp
    stop_grace_period: 45s
    healthcheck:
      test: ["CMD", "curl", "-fsS", "http://127.0.0.1:5000/readyz"]
      interval: 30s
      timeout: 5s
      start_period: 45s
      retries: 3
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "5"
